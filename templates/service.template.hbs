import {{titleCase name}} from "../model/{{snakeCase name}}.model";
import { supabase } from "../utils/supabase";

// export function create{{titleCase name}}(input: Partial<{{titleCase name}}>) {
//   return UserModel.create(input);
// }

export async function create{{titleCase name}}(body:any) {
  const response = await supabase
  .from<{{titleCase name}}>('{{name}}') // Message maps to the type of the row in your database.
  .select('*')
response.data // Response data will be of type Array<Message>.
  return response;
}